/*---------------------------------------------------------------------------------------------
 *  GENERATED by scripts/workbench/generate-settings.mjs — do not edit.
 *
 *  Every vgai settings key, as the `configuration` contribution VS Code's Settings editor
 *  reads. Derived from `packages/project/schemas/vgai-settings.schema.json`, which
 *  `npm run generate-schema` writes from the Zod settings document and
 *  `@vgai/project/settings/keys` walks at runtime — so the keys the editor READS and the keys
 *  a person can SEE are one derivation, not two lists.
 *
 *  SCOPE is WINDOW for every key: the layers are the person's (USER) and the project's
 *  (WORKSPACE, `.vscode/settings.json`), which is exactly what WINDOW permits. RESOURCE would
 *  offer a per-folder value no vgai reader asks for, and APPLICATION/MACHINE would put the
 *  project's own look out of the project's reach.
 *
 *  THERE ARE NO DEFAULTS here, and that is the ruling (ARCHITECTURE-CORE §The core is
 *  Code-OSS, U7): a default sits BELOW the user layer, so declaring a skew's look as one
 *  would let a person's cross-project palette defeat it. The adapter's declaration goes to
 *  the MEMORY target instead — see `vgaiSettings.ts`.
 *
 *  Regenerate:  node scripts/workbench/generate-settings.mjs --write
 *  Source (sha256 at generation; the generator refuses when it has moved):
 *    5e3885949d5f2bb1cc725bdd45223e9697eed2ba1ca435a909699c5f1a414c9a  packages/project/schemas/vgai-settings.schema.json
 *  16 keys.
 *--------------------------------------------------------------------------------------------*/

import { localize } from '../../../../nls.js';
import { ConfigurationScope, IConfigurationNode } from '../../../../platform/configuration/common/configurationRegistry.js';

/** Every vgai settings key, in the settings document's own declaration order. */
export const VGAI_SETTING_KEYS: readonly string[] = [
	'vgai.appearance.palette',
	'vgai.appearance.material',
	'vgai.appearance.icons',
	'vgai.appearance.regions.workspaceTabs',
	'vgai.appearance.regions.header',
	'vgai.appearance.regions.shelf',
	'vgai.appearance.regions.inspector',
	'vgai.appearance.regions.drawer',
	'vgai.appearance.regions.telemetry',
	'vgai.appearance.regions.hierarchyTypeSuffix',
	'vgai.appearance.regions.hierarchyInstanceRule',
	'vgai.appearance.regions.hierarchyRestrictions',
	'vgai.keymap',
	'vgai.play.keepPanelsVisible',
	'vgai.devicePreview.preset',
	'vgai.devicePreview.touch',
];

export const VGAI_CONFIGURATION_NODE: IConfigurationNode = {
	id: 'vgai',
	order: 100,
	title: localize('vgaiConfigurationTitle', "vgai"),
	type: 'object',
	properties: {
		'vgai.appearance.palette': {
			type: 'string',
			description: localize('vgai.appearance.palette', "Editor palette id — a built-in or a custom theme document."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.material': {
			type: 'string',
			description: localize('vgai.appearance.material', "Editor material id (`classic`, `glass`, or one a style bundle carries)."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.icons': {
			type: 'string',
			description: localize('vgai.appearance.icons', "Editor icon set id (`default`, or one a style bundle carries)."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.workspaceTabs': {
			type: 'string',
			enum: ['shown', 'hidden'],
			description: localize('vgai.appearance.regions.workspaceTabs', "The workspace tab strip in the top bar (Blender's); hidden unless a style shows it."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.header': {
			type: 'string',
			enum: ['shown', 'hidden'],
			description: localize('vgai.appearance.regions.header', "The top header bar."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.shelf': {
			type: 'string',
			enum: ['shown', 'hidden'],
			description: localize('vgai.appearance.regions.shelf', "The utility shelf."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.inspector': {
			type: 'string',
			enum: ['column', 'properties', 'card'],
			description: localize('vgai.appearance.regions.inspector', "Inspector presentation: the narrow docked column or a properties panel."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.drawer': {
			type: 'string',
			enum: ['shown', 'hidden'],
			description: localize('vgai.appearance.regions.drawer', "The bottom drawer."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.telemetry': {
			type: 'string',
			enum: ['shown', 'hidden'],
			description: localize('vgai.appearance.regions.telemetry', "The top bar's runtime telemetry cluster: the frame-rate readout, its sparkline and the audio meter. The Profiler utility is the same information's other door."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.hierarchyTypeSuffix': {
			type: 'string',
			enum: ['shown', 'hidden'],
			description: localize('vgai.appearance.regions.hierarchyTypeSuffix', "The `·TypeName` a component-instance row prints after its name in the hierarchy. Hidden leaves the row its name alone, the way Blender's Outliner does; the type stays reachable through the row's own tooltip and the inspector."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.hierarchyInstanceRule': {
			type: 'string',
			enum: ['shown', 'hidden'],
			description: localize('vgai.appearance.regions.hierarchyInstanceRule', "The dotted rule a component-instance row draws under its name in the hierarchy. Hidden leaves the name unmarked, the way Blender's Outliner does; the fact stays reachable through the row's own tooltip, its source actions and the inspector."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.appearance.regions.hierarchyRestrictions': {
			type: 'string',
			enum: ['select+viewport', 'viewport+render'],
			description: localize('vgai.appearance.regions.hierarchyRestrictions', "Which restriction columns a hierarchy row carries at its right edge: this editor's selection lock beside the eye, or Blender's default filter — the eye in its own column with the render column reserved and blank, three.js having one visibility flag for viewport and render alike."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.keymap': {
			type: 'string',
			description: localize('vgai.keymap', "Active keymap preset id (`vgai`, `blender`, …)."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.play.keepPanelsVisible': {
			type: 'boolean',
			description: localize('vgai.play.keepPanelsVisible', "Keep editor panels visible when starting Play, instead of letting an immersive workspace hide them. Turning this on during Play restores the panels; turning it off applies on the next Play."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.devicePreview.preset': {
			type: 'string',
			description: localize('vgai.devicePreview.preset', "Device preview frame preset id."),
			scope: ConfigurationScope.WINDOW,
		},
		'vgai.devicePreview.touch': {
			description: localize('vgai.devicePreview.touch', "Touch emulation override; null follows the preset."),
			scope: ConfigurationScope.WINDOW,
		},
	},
};
