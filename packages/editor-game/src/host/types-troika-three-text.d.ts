/**
 * Ambient declaration for `troika-three-text`, which ships no types. It
 * enters the editor only through the browser dependency table
 * (`served-bundle-runtime-modules.ts`) so a hosted project importing it resolves;
 * the editor never calls into it.
 */
declare module 'troika-three-text';
